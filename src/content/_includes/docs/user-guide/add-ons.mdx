import DocLink from '@components/DocLink.astro';
import { Aside } from '@astrojs/starlight/components';
import { Products } from '~/models/site.models';

ThingsBoard offers add-on products that extend the core platform with specialized capabilities. Each add-on integrates tightly with the main ThingsBoard instance and can be deployed alongside it.

| Add-on | Description |
|--------|-------------|
| **<DocLink product={props.product} path="user-guide/edge-computing">Edge Computing</DocLink>** | Run ThingsBoard at the edge — on gateways or local servers — for local data processing, offline operation, and seamless cloud synchronization |
| **<DocLink product={props.product} path="user-guide/trendz-analytics">Trendz Analytics</DocLink>** | Advanced analytics engine for building prediction models and anomaly detection on top of ThingsBoard data |

{props.product === Products.CE && (
<Aside type="note">
Some add-on features require **ThingsBoard Professional Edition**. See each add-on page for details on CE/PE availability.
</Aside>
)}
